# Contributing

ClawFlow welcomes contributions to Agent Runtime, Tool Sandbox, Permission Governance, Memory Layer, Observability, Plugin Registry, RAG, Multi-agent Collaboration, documentation, tests, and example applications.

## Development

```bash
make install
make test
make app
make benchmark
```

All new Example Applications must use `AgentRuntime`; isolated `demo.py` scripts are not accepted.


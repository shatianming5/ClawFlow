# Research Assistant

This application validates ClawFlow as infrastructure. It runs through `AgentRuntime`, uses the local planner, calls registered file/report tools, writes trace/checkpoint records, updates memory, and generates real artifacts in `outputs/`.


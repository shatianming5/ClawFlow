# Multi-agent Project Analysis

This application validates role-based agent collaboration through one runtime-triggered tool. It produces `outputs/multi_agent_report.md` and records handoff events in trace.

